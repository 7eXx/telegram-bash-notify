# TELEGRAM BASH NOTIFY
This script sends a Telegram message on exit.

## Precheck 
Create env.local file and fill it with the following variables:

```bash
# .env.local

BOT_TOKEN=
CHAT_ID=
```

## Usage

```bash
bash start.sh
```
